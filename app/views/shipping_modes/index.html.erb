<h2>Modalidades de transporte</h2>
<br/>
<%= link_to 'Cadastrar nova modalidade de transporte', new_shipping_mode_path %>

<% @shipping_modes.active.each do |sm| %>
  <h4><%= link_to sm.name, shipping_mode_path(sm.id) %></h4>
  <dl>
    <dt>Intervalo de distância:</dt>
    <dd><%= sm.min_distance %>-<%= sm.max_distance %> km</dd>
    <dt>Intervalo de peso:</dt>
    <dd><%= number_with_precision(sm.min_weight.to_f/1000) %>-<%= number_with_precision(sm.max_weight.to_f/1000, precision: 0) %> kg</dd>
    <dt>Taxa fixa:</dt>
    <dd><%= number_to_currency(sm.fixed_fee) %></dd>
  
  </dl>
<% end %>

<% if @shipping_modes.empty? %>
  Não há modalidades de transporte disponíveis
<% end %>